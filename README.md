# Calorio API

REST API для сервиса подсчёта калорий с функциями распознавания блюд по фотографии, управления целями КБЖУ и системой подписок.

## 🚀 Возможности

- ✅ Регистрация и аутентификация пользователей (JWT)
- ✅ Управление профилем пользователя
- ✅ Создание и редактирование блюд с КБЖУ
- ✅ Распознавание блюд по фотографии (AI)
- ✅ Установка и отслеживание целей КБЖУ
- ✅ Автоматический расчёт целей на основе параметров пользователя
- ✅ Группировка блюд по приёмам пищи (завтрак, обед, ужин, перекус)
- ✅ Статистика за день с прогрессом выполнения целей
- ✅ Система подписок с различными тарифными планами
- ✅ Интеграция с платёжными системами
- ✅ Swagger/OpenAPI документация

## 📋 Требования

- Python 3.11+
- PostgreSQL 15+ (для production) или SQLite (для development)
- Redis 7+ (опционально, для кэширования)
- Docker и Docker Compose (опционально)

## 🛠 Установка и запуск

### Локальная разработка (без Docker)

1. Клонируйте репозиторий:
```bash
git clone https://github.com/yourusername/calorio.git
cd calorio
```

2. Создайте виртуальное окружение и активируйте его:
```bash
python -m venv venv
source venv/bin/activate  # для Linux/Mac
# или
venv\Scripts\activate  # для Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

5. Отредактируйте `.env` файл и установите необходимые переменные окружения:
```env
SECRET_KEY=your-generated-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
OPENROUTER_API_KEY=your-openrouter-api-key
PAYMENT_WEBHOOK_SECRET=your-webhook-secret
```

6. Примените миграции:
```bash
python manage.py migrate
```

7. Создайте суперпользователя (опционально):
```bash
python manage.py createsuperuser
```

8. Соберите статические файлы:
```bash
python manage.py collectstatic
```

9. Запустите сервер разработки:
```bash
python manage.py runserver
```

API будет доступно по адресу: http://localhost:8000/api/

### Запуск с Docker Compose (рекомендуется)

1. Клонируйте репозиторий:
```bash
git clone https://github.com/yourusername/calorio.git
cd calorio
```

2. Создайте файл `.env`:
```bash
cp .env.example .env
```

3. Отредактируйте `.env` файл

4. Запустите контейнеры:
```bash
docker-compose up -d
```

5. Примените миграции:
```bash
docker-compose exec web python manage.py migrate
```

6. Создайте суперпользователя:
```bash
docker-compose exec web python manage.py createsuperuser
```

API будет доступно по адресу: http://localhost:8000/api/

## 📚 Документация API

После запуска приложения документация доступна по следующим адресам:

- **Swagger UI**: http://localhost:8000/api/docs/
- **ReDoc**: http://localhost:8000/api/redoc/
- **OpenAPI Schema**: http://localhost:8000/api/schema/

## 🧪 Тестирование

Запуск всех тестов:
```bash
pytest
```

Запуск с покрытием кода:
```bash
pytest --cov=. --cov-report=html
```

Запуск линтеров:
```bash
flake8 .
black --check .
isort --check-only .
```

## 🔒 Безопасность

### Переменные окружения

Все секретные данные должны храниться в переменных окружения:

- `SECRET_KEY` - секретный ключ Django (обязательно)
- `DEBUG` - режим отладки (False для production)
- `ALLOWED_HOSTS` - разрешённые хосты
- `OPENROUTER_API_KEY` - API ключ для распознавания изображений
- `PAYMENT_WEBHOOK_SECRET` - секрет для проверки webhook платежей
- `DATABASE_URL` - URL подключения к базе данных (для production)

### Генерация SECRET_KEY

```bash
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

### Генерация PAYMENT_WEBHOOK_SECRET

```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

## 🚀 Деплой на production

### Подготовка

1. Установите все переменные окружения
2. Установите `DEBUG=False`
3. Настройте `ALLOWED_HOSTS` с вашими доменами
4. Настройте `CORS_ALLOWED_ORIGINS` с доменами фронтенда
5. Используйте PostgreSQL вместо SQLite
6. Настройте SSL сертификаты
7. Настройте Nginx как reverse proxy

### Docker Compose для production

```bash
docker-compose -f docker-compose.yml up -d
```

### Health Checks

- `/api/health/` - проверка работоспособности всех компонентов
- `/api/ready/` - проверка готовности приложения (для Kubernetes)
- `/api/alive/` - проверка, что приложение не зависло (для Kubernetes)

## 📊 Мониторинг

### Логи

Логи записываются в:
- Консоль (для Docker)
- Файл `logs/django.log`

### Уровни логирования

Уровень логирования настраивается через переменную окружения `LOG_LEVEL`:
- `DEBUG` - все сообщения
- `INFO` - информационные сообщения и выше
- `WARNING` - предупреждения и ошибки
- `ERROR` - только ошибки

## 🏗 Архитектура

```
calorio/
├── calorio_api/          # Основной проект Django
│   ├── settings.py       # Настройки
│   ├── urls.py           # Главные URL маршруты
│   └── wsgi.py           # WSGI конфигурация
├── users/                # Приложение пользователей
│   ├── models.py         # Модели User, Profile
│   ├── views.py          # Views для аутентификации
│   └── serializers.py    # Сериализаторы
├── core/                 # Основное приложение
│   ├── models.py         # Модели Dish, Meal, DailyGoal
│   ├── views.py          # Views для блюд и целей
│   └── services.py       # Бизнес-логика
├── subscriptions/        # Приложение подписок
│   ├── models.py         # Модели Subscription, Payment
│   └── views.py          # Views для подписок
├── Dockerfile            # Docker образ
├── docker-compose.yml    # Docker Compose конфигурация
├── requirements.txt      # Python зависимости
└── README.md             # Документация

```

## 🤝 Вклад в проект

1. Fork репозиторий
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📝 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 📧 Контакты

- Email: support@calorio.app
- Website: https://calorio.app

## 🔄 Changelog

См. [CHANGELOG.md](CHANGELOG.md) для истории изменений.
