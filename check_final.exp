#!/usr/bin/expect -f

set timeout 60

spawn ssh root@217.26.29.106
expect "password:"
send "C&6!wMqm!wOZ\r"
expect "#"

send "journalctl -u calorio -n 20 --no-pager\r"
expect "#"

send "systemctl status calorio --no-pager\r"
expect "#"

send "curl -v http://localhost/api/health/ 2>&1 | head -n 30\r"
expect "#"

send "exit\r"
expect eof

