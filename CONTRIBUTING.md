# Руководство по внесению вклада в Calorio API

Спасибо за интерес к проекту! Мы приветствуем любой вклад.

## Как внести вклад

### 1. Сообщение об ошибках

Если вы нашли ошибку:

1. Проверьте, что ошибка ещё не была сообщена в [Issues](https://github.com/yourusername/calorio/issues)
2. Создайте новый Issue с подробным описанием:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Версия Python и Django
   - Логи ошибок (если есть)

### 2. Предложение новых функций

1. Создайте Issue с описанием функции
2. Объясните, зачем эта функция нужна
3. Дождитесь обсуждения и одобрения

### 3. Pull Requests

#### Процесс

1. Fork репозиторий
2. Создайте feature branch от `develop`:
   ```bash
   git checkout -b feature/my-feature develop
   ```
3. Внесите изменения
4. Добавьте тесты для новой функциональности
5. Убедитесь, что все тесты проходят:
   ```bash
   pytest
   ```
6. Проверьте код линтерами:
   ```bash
   flake8 .
   black --check .
   isort --check-only .
   ```
7. Commit изменения с понятным сообщением:
   ```bash
   git commit -m "feat: add user profile export"
   ```
8. Push в ваш fork:
   ```bash
   git push origin feature/my-feature
   ```
9. Создайте Pull Request в `develop` ветку

#### Требования к Pull Request

- [ ] Код соответствует PEP 8 (проверено flake8)
- [ ] Код отформатирован black и isort
- [ ] Добавлены тесты для новой функциональности
- [ ] Все тесты проходят
- [ ] Покрытие кода тестами не уменьшилось
- [ ] Документация обновлена (если требуется)
- [ ] CHANGELOG.md обновлён
- [ ] Commit сообщения следуют [Conventional Commits](https://www.conventionalcommits.org/)

#### Формат commit сообщений

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <subject>

<body>

<footer>
```

Типы:
- `feat`: новая функция
- `fix`: исправление ошибки
- `docs`: изменения в документации
- `style`: форматирование кода
- `refactor`: рефакторинг кода
- `test`: добавление тестов
- `chore`: изменения в сборке или инструментах

Примеры:
```
feat(auth): add password reset functionality
fix(dishes): correct KBJU calculation for composite dishes
docs(api): update Swagger documentation for goals endpoint
```

### 4. Стиль кода

#### Python

- Следуйте PEP 8
- Используйте type hints где возможно
- Максимальная длина строки: 127 символов
- Используйте docstrings для функций и классов

Пример:

```python
def calculate_calories(
    weight: int,
    height: int,
    age: int,
    gender: str,
    activity_level: str
) -> int:
    """
    Рассчитывает дневную норму калорий.
    
    Args:
        weight: Вес в килограммах
        height: Рост в сантиметрах
        age: Возраст в годах
        gender: Пол ('male' или 'female')
        activity_level: Уровень активности
        
    Returns:
        Дневная норма калорий
        
    Raises:
        ValueError: Если параметры некорректны
    """
    # Реализация
    pass
```

#### Django

- Используйте class-based views где возможно
- Выносите бизнес-логику в services
- Используйте сериализаторы DRF для валидации
- Добавляйте docstrings к ViewSets и сериализаторам

#### Тесты

- Пишите тесты для всей новой функциональности
- Используйте pytest fixtures
- Именуйте тесты понятно: `test_user_can_create_dish`
- Группируйте тесты по функциональности

Пример:

```python
import pytest
from django.contrib.auth import get_user_model

User = get_user_model()


@pytest.mark.django_db
class TestUserRegistration:
    def test_user_can_register_with_valid_data(self, api_client):
        """Пользователь может зарегистрироваться с корректными данными"""
        data = {
            'email': 'test@example.com',
            'password': 'testpass123',
            'first_name': 'Test'
        }
        response = api_client.post('/api/auth/register/', data)
        
        assert response.status_code == 201
        assert 'tokens' in response.data
        assert User.objects.filter(email='test@example.com').exists()
```

### 5. Локальная разработка

#### Установка

```bash
# Клонирование репозитория
git clone https://github.com/yourusername/calorio.git
cd calorio

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt

# Создание .env файла
cp .env.example .env

# Применение миграций
python manage.py migrate

# Запуск сервера
python manage.py runserver
```

#### Запуск тестов

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=.

# Только unit тесты
pytest -m unit

# Только integration тесты
pytest -m integration
```

#### Линтеры

```bash
# Проверка
flake8 .
black --check .
isort --check-only .

# Автоматическое форматирование
black .
isort .
```

### 6. Документация

При добавлении новых эндпоинтов:

1. Добавьте docstring к ViewSet
2. Используйте `@extend_schema` из drf-spectacular
3. Обновите REST API документацию в `.cursor/rules/rest-api-docs.mdc`
4. Добавьте примеры использования

Пример:

```python
from drf_spectacular.utils import extend_schema, OpenApiParameter
from rest_framework import viewsets

class DishViewSet(viewsets.ModelViewSet):
    """
    ViewSet для управления блюдами.
    
    Позволяет создавать, просматривать, редактировать и удалять блюда.
    """
    
    @extend_schema(
        summary="Создание нового блюда",
        description="Создаёт новое блюдо с указанными КБЖУ",
        request=DishSerializer,
        responses={201: DishSerializer},
        tags=['Блюда']
    )
    def create(self, request):
        # Реализация
        pass
```

### 7. Code Review

При review Pull Request обращайте внимание на:

- Соответствие требованиям
- Качество кода
- Покрытие тестами
- Производительность
- Безопасность
- Документацию

### 8. Вопросы и поддержка

Если у вас есть вопросы:

1. Проверьте [документацию](README.md)
2. Поищите в [Issues](https://github.com/yourusername/calorio/issues)
3. Создайте новый Issue с вопросом
4. Напишите на support@calorio.app

## Лицензия

Внося вклад в проект, вы соглашаетесь, что ваш код будет лицензирован под MIT License.

## Благодарности

Спасибо всем, кто вносит вклад в проект! 🎉

