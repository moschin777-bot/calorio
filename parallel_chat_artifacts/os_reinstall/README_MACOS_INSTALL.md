# Автоматическая установка macOS 13/14

## ✅ Что уже сделано автоматически:

1. ✅ Созданы скрипты для автоматизации процесса
2. ✅ Открыта страница загрузки OpenCore Legacy Patcher
3. ✅ Подготовлена рабочая директория: `~/macos_installer_auto`

## 🚀 Быстрый старт:

### Вариант 1: Автоматический (после загрузки OCLP)

```bash
./complete_macos_install.sh
```

Скрипт автоматически:
- Найдет загруженный файл OCLP
- Установит его в /Applications
- Запустит приложение

### Вариант 2: Ручная загрузка

1. Страница загрузки уже открыта в браузере
2. Скачайте файл `.dmg` с GitHub
3. Запустите скрипт:
   ```bash
   ./complete_macos_install.sh
   ```

## 📋 Полный процесс установки:

### Шаг 1: Загрузка OpenCore Legacy Patcher
- Страница: https://github.com/dortania/OpenCore-Legacy-Patcher/releases/latest
- Скачайте файл `OpenCore-Patcher-GUI.app.dmg` (или похожий)

### Шаг 2: Автоматическая установка OCLP
```bash
./complete_macos_install.sh
```

### Шаг 3: В OpenCore Legacy Patcher
1. Нажмите **"Create macOS Installer"**
2. Выберите **macOS 13 Ventura** (рекомендуется) или **macOS 14 Sonoma**
3. OCLP автоматически загрузит установщик macOS (30-60 минут)
4. Подключите USB-накопитель (16GB+)
5. Создайте загрузочный установщик

### Шаг 4: Установка macOS
1. Перезагрузите Mac, удерживая **Option (Alt)**
2. Выберите загрузку с USB
3. Следуйте инструкциям установщика
4. **ВАЖНО:** После установки НЕ перезагружайтесь сразу!
5. Запустите OCLP и примените **"Post Install Root Patch"**
6. Перезагрузите Mac

## ⚠️ ВАЖНО:

- **Создайте резервную копию через Time Machine перед началом!**
- MacBook9,1 официально не поддерживает macOS 13/14
- Установка возможна только через OCLP
- Процесс может занять 1-2 часа

## 📁 Созданные файлы:

- `complete_macos_install.sh` - полная автоматическая установка
- `install_macos_automated.sh` - альтернативный скрипт
- `install_macos_simple.sh` - простой скрипт
- `INSTALL_MACOS_13_14_GUIDE.md` - подробная инструкция

## 🔧 Если что-то пошло не так:

1. Восстановите из Time Machine (Command+R при загрузке)
2. Или переустановите macOS 12 через Recovery Mode

## ✅ Готово к использованию!

Запустите `./complete_macos_install.sh` после загрузки OCLP.

