# Инструкция по применению исправлений

## ✅ Что исправлено:

1. **Убраны нули из полей ввода** - поля теперь пустые по умолчанию
2. **Исправлена обработка пустых значений** - корректная валидация и преобразование
3. **Исправлена ошибка с датой** - дата передаётся в правильном формате YYYY-MM-DD
4. **Улучшено распознавание блюд** - более надёжный парсинг JSON из ответа OpenRouter
5. **Обновлены формулы КБЖУ** - более точный расчёт белков на основе веса

## ✅ Изменения закоммичены и запушены в Git!

Все исправления уже в репозитории. Теперь нужно применить их на сервере.

## 📤 Как применить изменения на сервере:

### Вариант 1: Через Git (рекомендуется) ✅

**Изменения уже в Git!** Выполните на сервере:

```bash
ssh root@217.26.29.106
cd /var/www/calorio
git pull
cd frontend
npm install --legacy-peer-deps
npm run build
cd ..
source venv/bin/activate
pip install -q requests
python manage.py collectstatic --noinput
deactivate
sudo systemctl restart calorio
sudo systemctl reload nginx
```

Или используйте готовый скрипт:
```bash
cd /var/www/calorio
./scripts/deploy_server.sh
```

### Вариант 2: Прямая загрузка (если Git не работает)

```bash
# 1. Загрузите фронтенд
rsync -avz --delete \
  --exclude 'node_modules' \
  frontend/dist/ \
  root@217.26.29.106:/var/www/calorio/frontend/dist/

# 2. Загрузите изменения в core
scp core/views.py root@217.26.29.106:/var/www/calorio/core/
scp core/utils.py root@217.26.29.106:/var/www/calorio/core/
scp requirements.txt root@217.26.29.106:/var/www/calorio/
scp calorio_api/settings.py root@217.26.29.106:/var/www/calorio/calorio_api/

# 3. На сервере выполните:
ssh root@217.26.29.106
cd /var/www/calorio
source venv/bin/activate
pip install -q requests
python manage.py collectstatic --noinput
deactivate
sudo systemctl restart calorio
sudo systemctl reload nginx
```

## 🔍 Проверка после загрузки:

1. Откройте http://217.26.29.106/
2. Попробуйте открыть форму расчёта целей - поля должны быть пустыми
3. Введите параметры и нажмите "Рассчитать" - должно работать без ошибки с датой
4. Попробуйте распознать блюдо - должно работать с OpenRouter API

## 📝 Изменённые файлы:

- `frontend/src/components/CalculateGoalModal.tsx` - исправлена форма расчёта
- `frontend/src/components/RecognizeDishModal.tsx` - исправлена передача base64
- `core/views.py` - улучшено распознавание и исправлен DayDataView
- `core/utils.py` - обновлены формулы КБЖУ
- `core/serializers.py` - без изменений (уже был правильный)
- `requirements.txt` - добавлен requests
- `calorio_api/settings.py` - добавлен SITE_URL

## ⚠️ Важно:

- Убедитесь, что на сервере установлен `OPENROUTER_API_KEY` в `.env`
- Проверьте, что `requests` установлен: `pip install requests`
- После загрузки очистите кэш браузера (Ctrl+Shift+R)

