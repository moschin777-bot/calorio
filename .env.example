# Django Settings
# ВАЖНО: Сгенерируйте новый SECRET_KEY для production!
# Можно использовать: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here-change-this-in-production

# Debug режим (False для production!)
DEBUG=True

# Разрешённые хосты (через запятую, без пробелов)
# Для production: ALLOWED_HOSTS=api.calorio.app,www.calorio.app
ALLOWED_HOSTS=localhost,127.0.0.1

# OpenRouter API (для распознавания блюд)
OPENROUTER_API_KEY=your-openrouter-api-key-here

# Payment Provider Webhook Secret (для проверки подписи webhook)
# Сгенерируйте случайную строку: python -c "import secrets; print(secrets.token_urlsafe(32))"
PAYMENT_WEBHOOK_SECRET=your-webhook-secret-here

# CORS Settings (добавьте домены вашего фронтенда через запятую)
# Для production: CORS_ALLOWED_ORIGINS=https://calorio.app,https://www.calorio.app
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Security Settings (только для production)
# SECURE_SSL_REDIRECT=True
# SECURE_HSTS_SECONDS=31536000

# Data Upload Limits
# DATA_UPLOAD_MAX_MEMORY_SIZE=10485760  # 10 МБ
# DATA_UPLOAD_MAX_NUMBER_FIELDS=1000

# Database (для production можно использовать PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost:5432/calorio

# Email Settings (для отправки уведомлений, опционально)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
